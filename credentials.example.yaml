# secrets/credentials.example.yaml — copy this to credentials.yaml and fill in
# your real Pasqal Cloud account details. credentials.yaml is gitignored;
# this example file is safe to commit and must only ever contain placeholders.
username: "you@example.com"
password: "your-pasqal-cloud-password"
project_id: "00000000-0000-0000-0000-000000000000"
